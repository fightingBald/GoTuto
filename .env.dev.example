# For local CLI or docker-compose fallback (not used by k8s directly)
POSTGRES_DB=productdb
POSTGRES_USER=app
POSTGRES_PASSWORD=app_password
DATABASE_URL=postgres://app:app_password@localhost:5432/productdb?sslmode=disable
HTTP_ADDRESS=:8080
LOG_LEVEL=debug

